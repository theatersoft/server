{
  "name": "test",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "_debug": "npm run _test -- --inspect --debug-brk",
    "_test": "BUS=`[ -f .bus ] && cat .bus` AUTH=`[ -f .auth ] && cat .auth` node",
    "auth": "npm run _test -- auth.js",
    "clear-cookies": "echo '{}' >.cookies.json",
    "config": "npm run _test -- config.js",
    "login": "npm run _test -- login.js",
    "debug-auth": "npm run _debug -- auth.js",
    "debug-config": "npm run _debug -- config.js",
    "debug-login": "npm run _debug -- login.js"
  },
  "author": "Rob Huehn <rob@theatersoft.com>",
  "private": true,
  "license": "AGPL-3.0",
  "dependencies": {},
  "devDependencies": {
    "tough-cookie-filestore": "0.0.1"
  }
}
